# Core packages
pyqt5
yt-dlp
requests
tqdm
pydub
pandas
numpy
psutil
moviepy
matplotlib
scikit-learn
opencv-python
Pillow
PyYAML

# Optional or less common requirements (Add/remove as truly used)
redis
psycopg2
pymongo
qiskit
qiskit-ibm-runtime

# For FastAPI-powered UIs (if used)
fastapi
uvicorn

# Audio/video/image processing (if relevant)
torch
torchvision
diffusers

# PDF/Doc manipulation (if used)
python-docx
pymupdf

# Security
cryptography
bcrypt
pyjwt

# For Windows service support (if used)
pywin32

# Other
bottle>=0.12.20  # Pinned if required


pydantic>=2.10,<3.0  # Pinned to avoid breaking changes in v3.0
